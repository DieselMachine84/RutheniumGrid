<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:rut="clr-namespace:Ruthenium;assembly=Ruthenium.DataGrid"
        x:Class="Ruthenium.TestApp.MainWindow"
        Title="Ruthenium DataGrid Test">
        <rut:DataGrid Name="grid" ItemsSource="{Binding}">
                <rut:DataGrid.Columns>
                        <rut:DataGridColumn FieldName="Id" />
                        <rut:DataGridColumn FieldName="Text" />
                </rut:DataGrid.Columns>
        </rut:DataGrid>
</Window>